{
  "name": "gym-tracker-emulator-tests",
  "private": true,
  "description": "Integrationstests gegen den Firebase-Emulator (Auth, Firestore, Storage) – siehe README.md",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-gym --config ../../firebase.json \"node --test social.emulator.test.mjs\"",
    "test:ui": "firebase emulators:exec --project demo-gym --config ../../firebase.json \"node --test ui.test.mjs\""
  },
  "devDependencies": {
    "firebase": "12.3.0",
    "firebase-tools": "^14.0.0",
    "playwright": "^1.56.0"
  }
}
